document.querySelectorAll('[data-interaction="trust"]').forEach(box=>{let attempts=0;const feedback=box.parentElement.querySelector('.feedback'),hint=box.parentElement.querySelector('.hint-box');box.querySelectorAll('button').forEach(button=>button.addEventListener('click',()=>{if(button.dataset.answer==='right'){feedback.textContent='Yes. It identifies a record a reader can locate.';hint.classList.remove('visible')}else{attempts++;feedback.textContent=attempts===1?'Try again: find the card with a traceable maker and location.':'Pause and use the clue.';if(attempts>1)hint.classList.add('visible')}}));box.closest('.slide').addEventListener('show-answer',()=>{feedback.textContent='Answer: the named council meeting notes.';hint.classList.add('visible')})});
