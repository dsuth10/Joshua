const path=require('path');const {pathToFileURL}=require('url');const {chromium}=require('playwright');
const dir=path.resolve(__dirname,'..'),input=path.join(dir,'Lesson_26_Shade_Shed_Reading_Article.html'),output=path.join(dir,'Lesson_26_Shade_Shed_Reading_Article.pdf');
(async()=>{const browser=await chromium.launch({headless:true});try{const page=await browser.newPage({viewport:{width:1440,height:1100}});const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});page.on('pageerror',e=>errors.push(e.message));await page.goto(pathToFileURL(input).href,{waitUntil:'networkidle'});await page.emulateMedia({media:'print'});const audit=await page.evaluate(()=>({pages:document.querySelectorAll('.page').length,overflow:[...document.querySelectorAll('.page')].map((p,i)=>({page:i+1,x:p.scrollWidth-p.clientWidth,y:p.scrollHeight-p.clientHeight})).filter(x=>x.x>1||x.y>1)}));if(audit.pages!==2||audit.overflow.length||errors.length)throw Error(JSON.stringify({audit,errors}));await page.pdf({path:output,format:'A4',preferCSSPageSize:true,printBackground:true,margin:{top:'0',right:'0',bottom:'0',left:'0'}});console.log('PASS '+output)}finally{await browser.close()}})().catch(e=>{console.error(e);process.exitCode=1});
